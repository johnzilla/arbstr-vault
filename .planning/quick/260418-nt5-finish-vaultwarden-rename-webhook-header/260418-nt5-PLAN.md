---
id: 260418-nt5
slug: finish-vaultwarden-rename-webhook-header
description: finish vaultwarden rename — webhook header + active docs
date: 2026-04-18
mode: quick
---

# Quick Task 260418-nt5: Finish Vaultwarden → arbstr-vault Rename

## Context

Prior rename commits (98025ef, 9c0b6aa) covered source, config, env vars, and
the startup log. This task finishes the remaining residue: one HTTP header in
live source and six active planning docs. Shipped-milestone archives under
`.planning/milestones/**` are intentionally out of scope (preserve history).

## Locked Decisions

- **Webhook header:** `X-Vaultwarden-Signature` → `X-ArbstrVault-Signature`.
  Breaking change for any deployed webhook consumer. Accepted.
- **Archive scope:** `.planning/milestones/**` left untouched — historical
  record of what was written at the time.

## Tasks

### Task 1: Rename webhook signature header

**Files:** `src/modules/webhook/webhook.service.ts`

**Action:** Replace `X-Vaultwarden-Signature` with `X-ArbstrVault-Signature`.

**Verify:** `grep -r "Vaultwarden" src/` returns nothing.

**Done:** No `Vaultwarden` references remain in `src/`.

### Task 2: Update active planning doc titles

**Files:**
- `.planning/PROJECT.md` (line 1)
- `.planning/ROADMAP.md` (line 1)

**Action:** Remove the parenthetical `(Vaultwarden)` from each H1 title.

**Verify:** `grep -l "Vaultwarden" .planning/*.md` returns nothing.

**Done:** Active planning doc titles reflect the new name.

### Task 3: Update research/ docs

**Files:**
- `.planning/research/SUMMARY.md` (lines 3, 10)
- `.planning/research/STACK.md` (line 184)
- `.planning/research/ARCHITECTURE.md` (line 365)
- `.planning/research/FEATURES.md` (lines 174, 192)

**Action:** Replace `Vaultwarden` with `arbstr-vault` in prose. Preserve
meaning — line 10 of SUMMARY.md is a descriptive sentence; line 174 of
FEATURES.md names the product in a competitive comparison.

**Verify:** `grep -r "Vaultwarden" .planning/research/` returns nothing.

**Done:** Research docs use the new project name.

## Must Haves

- `grep -ri "vaultwarden" src/ .planning/*.md .planning/research/` returns zero matches
- `.planning/milestones/**` unchanged (archives preserved)
- `npm test` still passes (webhook-consuming tests may need a header update too)

## Out of Scope

- `.planning/milestones/**` (shipped archives — preserve history)
- README.md and other top-level docs (already clean per prior commits)
