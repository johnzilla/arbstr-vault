---
phase: 06-settle-release-and-verification
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - tests/integration/internal-billing.test.ts
autonomous: true
requirements:
  - BILL-05
  - BILL-06
  - BILL-07
  - BILL-08
  - BILL-09
  - BILL-10

must_haves:
  truths:
    - "Settling a reservation returns 200 with settled:true, refunded_msats, actual_msats, remaining_balance_msats"
    - "Settling reduces agent balance by actual cost, not reserved amount"
    - "Settling an already-settled reservation returns 200 without creating duplicate ledger entries"
    - "Releasing a reservation returns 200 with released:true and restores full reserved amount"
    - "Releasing an already-released reservation returns 200 without creating duplicate ledger entries"
    - "Settle and release return 404 for unknown reservation_id"
    - "Settle audit entry contains tokens_in, tokens_out, provider, latency_ms metadata"
    - "Full flow: deposit -> reserve -> settle -> balance reflects actual cost"
  artifacts:
    - path: "tests/integration/internal-billing.test.ts"
      provides: "Integration tests for settle, release, idempotency, and end-to-end flow"
      contains: "POST /internal/settle"
  key_links:
    - from: "tests/integration/internal-billing.test.ts"
      to: "src/routes/internal/reserve.routes.ts"
      via: "app.inject() calls to /internal/settle and /internal/release"
      pattern: "url.*internal/settle"
---

<objective>
Add comprehensive integration tests for POST /internal/settle and POST /internal/release routes.

Purpose: Verify settle, release, idempotency, audit metadata, and end-to-end deposit-reserve-settle-balance flow.
Output: Extended tests/integration/internal-billing.test.ts with settle and release test suites.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-settle-release-and-verification/06-CONTEXT.md
@.planning/phases/06-settle-release-and-verification/06-01-SUMMARY.md

<interfaces>
<!-- Settle and release route contracts from Plan 01 -->

POST /internal/settle
  Headers: x-internal-token: string
  Body: { reservation_id: string, actual_msats: number, tokens_in: number, tokens_out: number, provider: string, latency_ms: number }
  200: { settled: true, refunded_msats: number, actual_msats: number, remaining_balance_msats: number }
  404: { error: { code: "not_found", message: "Reservation not found" } }

POST /internal/release
  Headers: x-internal-token: string
  Body: { reservation_id: string }
  200: { released: true }
  404: { error: { code: "not_found", message: "Reservation not found" } }

Idempotency: Both settle and release return success on retry without duplicating entries.

Existing test setup from tests/integration/internal-billing.test.ts:
- TEST_INTERNAL_TOKEN = 'test-internal-token-min-32-characters-long'
- TEST_AGENT_TOKEN = 'vtk_test_agent_token_for_billing_reserved'
- Agent 'ag_test123' with 100,000 msats balance
- buildTestApp() creates in-memory DB with migrations
- setupTestAgent(app) inserts agent + deposit

Ledger entry verification pattern:
```typescript
const entry = app.db.select().from(schema.ledgerEntries).where(eq(schema.ledgerEntries.id, id)).get();
```

Audit log verification:
```typescript
import { auditLog } from '../../src/db/schema.js';
const audit = app.db.select().from(schema.auditLog).where(eq(schema.auditLog.ref_id, reservation_id)).get();
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add settle integration tests</name>
  <read_first>
    - tests/integration/internal-billing.test.ts
    - src/routes/internal/reserve.routes.ts
    - src/db/schema.ts
  </read_first>
  <files>tests/integration/internal-billing.test.ts</files>
  <action>
Add a new `describe('POST /internal/settle', ...)` block after the existing `describe('POST /internal/reserve', ...)` block in tests/integration/internal-billing.test.ts. Use the same beforeEach/afterEach pattern (buildTestApp, setupTestAgent, app.ready, app.close).

Add import for `auditLog` from schema if not present: ensure `* as schema` import covers it (it does -- use `schema.auditLog`).

Each test follows this pattern: first reserve funds, then settle (or attempt to settle).

Helper function at top of the new describe block:
```typescript
async function reserveFunds(app, amount: number = 50_000): Promise<string> {
  const res = await app.inject({
    method: 'POST', url: '/internal/reserve',
    headers: { 'x-internal-token': TEST_INTERNAL_TOKEN },
    payload: { agent_token: TEST_AGENT_TOKEN, amount_msats: amount, correlation_id: 'corr_settle_test', model: 'gpt-4o' },
  });
  return JSON.parse(res.body).reservation_id;
}
```

**Tests to add (7 tests):**

1. **"valid settle returns 200 with settled response"** — Reserve 50,000. Settle with actual_msats: 32,000. Assert 200, body has `settled: true`, `refunded_msats: 18000` (50000-32000), `actual_msats: 32000`, `remaining_balance_msats: 68000` (100000-32000).

2. **"settle inserts RELEASE and PAYMENT ledger entries atomically"** — Reserve 50,000, settle with actual_msats: 30,000. Query `schema.ledgerEntries` where `ref_id = reservation_id`. Expect 2 entries (not counting the original RESERVE which has ref_id = correlation_id). Filter entries: one with entry_type='RELEASE' and amount_msat=+50000, one with entry_type='PAYMENT' and amount_msat=-30000. Both should have mode='simulated'.

3. **"settle is idempotent - second settle returns same response"** — Reserve 50,000, settle with actual_msats: 20,000. Then settle again with same reservation_id and actual_msats: 20,000. Assert second response is 200 with `settled: true`. Then count RELEASE entries with ref_id = reservation_id -- should be exactly 1 (not 2). Count PAYMENT entries similarly -- exactly 1.

4. **"settle records audit metadata"** — Reserve 50,000, settle with actual_msats: 25,000, tokens_in: 150, tokens_out: 500, provider: 'openai', latency_ms: 1200. Query `schema.auditLog` where action='PAYMENT_SETTLED' and ref_id = reservation_id. Assert entry exists, metadata contains { tokens_in: 150, tokens_out: 500, provider: 'openai', latency_ms: 1200 }.

5. **"settle with unknown reservation_id returns 404"** — Call settle with reservation_id: 'tx_nonexistent'. Assert 404, body has `error.code: 'not_found'`.

6. **"settle with actual cost higher than reserved amount works correctly"** — Reserve 10,000, settle with actual_msats: 15,000. This is valid (actual can exceed reserve). Assert 200, `refunded_msats: -5000` (10000-15000, negative means overcharge), `remaining_balance_msats: 85000` (100000-15000).

7. **"balance after settle reflects actual cost not reserved amount"** — Reserve 80,000 (balance: 100,000 -> 20,000 after reserve). Settle with actual_msats: 30,000. After settle, RELEASE +80,000 and PAYMENT -30,000 are added. Net effect: balance = 100,000 - 30,000 = 70,000. Assert remaining_balance_msats: 70,000.
  </action>
  <verify>
    <automated>cd /home/john/vault/projects/github.com/vaultwarden && npx vitest run tests/integration/internal-billing.test.ts --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "POST /internal/settle" tests/integration/internal-billing.test.ts
    - grep -q "settled.*true" tests/integration/internal-billing.test.ts
    - grep -q "refunded_msats" tests/integration/internal-billing.test.ts
    - grep -q "idempotent" tests/integration/internal-billing.test.ts
    - grep -q "audit metadata" tests/integration/internal-billing.test.ts
    - grep -q "PAYMENT_SETTLED" tests/integration/internal-billing.test.ts
    - All settle tests pass (vitest exits 0)
  </acceptance_criteria>
  <done>7 settle integration tests pass covering happy path, atomicity, idempotency, audit metadata, 404, overcharge, and balance verification</done>
</task>

<task type="auto">
  <name>Task 2: Add release and end-to-end integration tests</name>
  <read_first>
    - tests/integration/internal-billing.test.ts
    - src/routes/internal/reserve.routes.ts
  </read_first>
  <files>tests/integration/internal-billing.test.ts</files>
  <action>
Add a `describe('POST /internal/release', ...)` block and a `describe('End-to-end billing flow', ...)` block after the settle tests in tests/integration/internal-billing.test.ts. Same beforeEach/afterEach pattern. Reuse the reserveFunds helper (move it to module scope above all describe blocks if needed).

**Release tests (5 tests):**

1. **"valid release returns 200 with released:true"** — Reserve 40,000. Release with reservation_id. Assert 200, body `{ released: true }`.

2. **"release restores full reserved amount to balance"** — Reserve 60,000 (balance: 100,000 -> 40,000). Release. Check balance via a second reserve attempt for 95,000 msats — it should succeed (balance restored to 100,000). Or more directly: after release, query `ledgerRepo.getBalance` or do another reserve for 100,000 and check it doesn't return 402.

3. **"release is idempotent - second release returns same response"** — Reserve 30,000. Release. Release again with same reservation_id. Assert second response is 200 with `released: true`. Count RELEASE entries with ref_id = reservation_id -- exactly 1.

4. **"release with unknown reservation_id returns 404"** — Call release with reservation_id: 'tx_nonexistent'. Assert 404, body has `error.code: 'not_found'`.

5. **"release inserts RELEASE ledger entry with correct values"** — Reserve 45,000. Release. Query ledger entries with ref_id = reservation_id and entry_type = 'RELEASE'. Assert exists, amount_msat = +45,000, mode = 'simulated'.

**End-to-end flow test (1 test):**

1. **"deposit -> reserve -> settle -> balance reflects actual cost"** — Start with agent at 100,000 balance.
   a. Reserve 50,000 (remaining: 50,000).
   b. Settle with actual_msats: 12,000, tokens_in: 100, tokens_out: 200, provider: 'anthropic', latency_ms: 800.
   c. Assert settle response: remaining_balance_msats: 88,000 (100,000 - 12,000).
   d. Reserve again for 85,000 — should succeed (balance is 88,000).
   e. Assert second reserve remaining_balance_msats: 3,000 (88,000 - 85,000).
  </action>
  <verify>
    <automated>cd /home/john/vault/projects/github.com/vaultwarden && npx vitest run tests/integration/internal-billing.test.ts --reporter=verbose 2>&1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "POST /internal/release" tests/integration/internal-billing.test.ts
    - grep -q "released.*true" tests/integration/internal-billing.test.ts
    - grep -q "End-to-end" tests/integration/internal-billing.test.ts
    - grep -q "deposit.*reserve.*settle" tests/integration/internal-billing.test.ts
    - All tests pass (vitest exits 0 with 0 failures)
    - Total test count is at least 21 (9 existing reserve + 7 settle + 5 release + 1 e2e = 22)
  </acceptance_criteria>
  <done>5 release tests, 1 end-to-end flow test pass; total test file covers reserve, settle, release, idempotency, and full billing lifecycle</done>
</task>

</tasks>

<verification>
- All tests pass: `npx vitest run tests/integration/internal-billing.test.ts`
- Test count is 22+ (9 reserve + 7 settle + 5 release + 1 e2e)
- Idempotency verified: duplicate settle/release does not create extra ledger entries
- Audit metadata verified: PAYMENT_SETTLED entry has tokens_in, tokens_out, provider, latency_ms
- Balance arithmetic verified: actual cost charged, not reserved amount
</verification>

<success_criteria>
- All 22+ integration tests pass
- Settle tests cover: happy path, atomic entries, idempotency, audit, 404, overcharge, balance
- Release tests cover: happy path, balance restoration, idempotency, 404, entry values
- End-to-end test covers: deposit -> reserve -> settle -> re-reserve flow
- Zero test failures
</success_criteria>

<output>
After completion, create `.planning/phases/06-settle-release-and-verification/06-02-SUMMARY.md`
</output>
